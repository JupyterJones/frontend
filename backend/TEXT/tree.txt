/your-project-root
  ├── babel.config.js        # Babel config for transpiling code
  ├── build                  # Folder for production build (typically generated by build scripts)
  ├── jest.config.ts         # Jest config file (for TypeScript and Jest setup)
  ├── jest.setup.ts          # Jest setup file (for Testing Library configuration)
  ├── node_modules           # Folder for your project dependencies
  ├── package.json           # Contains project metadata and dependencies
  ├── package-lock.json      # Locks the versions of your dependencies (generated after install)
  ├── package-lock_.json     # Another lock file (it’s fine, but you might want to clean this up)
  ├── public                 # Public directory (includes static assets like HTML files)
  ├── README.md              # Project README (for documentation)
  ├── src                    # Source code directory
  ├── tsconfig.json          # TypeScript config file
  └── YouTube_Assets         # Additional assets folder (perhaps related to your YouTube project)
